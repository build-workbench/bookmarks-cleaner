# 性能方法学

本页解释应该如何阅读站点中的性能主张。它刻意避免给出未经维护验证的“调优偏方”，而是聚焦当前运行时形态下真正成立的解释方式。

## 测量包络

站点里的性能数字至少要沿四个轴阅读：

| 轴 | 要回答的问题 |
|----|--------------|
| 冷启动 | 在重型智能层尚未加载之前，进入运行时的成本是多少？ |
| 稳态吞吐 | Pipeline 进入工作态之后，书签处理速度如何？ |
| 模式宽度 | 当前运行是仅规则、混合模式，还是带 LLM 协助？ |
| 资源压力 | 内存与并发限制如何决定实际可处理上限？ |

## 这些数字到底意味着什么

- **规则优先数字** 描述的是系统最便宜、最稳定的执行路径。
- **混合模式数字** 包含额外模型与语义工作，因此应被理解为“更强但更贵”的运行。
- **可选 LLM 参与** 应该被理解为对歧义样本的升级路径，而不是默认要求。

## 当前真正成立的优化杠杆

维护中的性能故事依赖下面这些与架构一致的杠杆：

1. **让常见路径保持确定性。** 越多书签在规则层就被解决，单次运行的边际成本就越低。
2. **避免不必要的智能宽度。** ML、语义与 LLM 路径都很有价值，但都不是零成本。
3. **保持本地执行。** 除非用户显式选择，否则不要把性能问题变成网络往返问题。
4. **把并发收益理解为工作负载相关。** 线程级提速取决于可并行比例，以及重型依赖在总耗时中的占比。

## 与其他性能页面的关系

- [并发处理](/zh/performance/concurrency) 讨论为何运行时采用当前并发模型。
- [缓存策略](/zh/performance/caching) 讨论复用机制到底能带来什么收益。
- [技术白皮书](/zh/whitepaper) 则把性能放回系统边界与失败模型中整体解释。
